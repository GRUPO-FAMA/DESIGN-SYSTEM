/** Hairline KPI grid (3–4 cells): Syne number + label. */
export interface KpisProps { items: {value: string; label: string}[]; style?: React.CSSProperties }
export declare function Kpis(props: KpisProps): JSX.Element;

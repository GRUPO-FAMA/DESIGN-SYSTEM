/** Google rating seal linking to the Maps listing. */
export interface RatingProps { value?: string; count?: string; href?: string; style?: React.CSSProperties }
export declare function Rating(props: RatingProps): JSX.Element;

Underline tabs in uppercase tracked labels over a hairline; the active tab carries a 2px black bar.

```jsx
<Tabs items={[{id:'mad',label:'Madrid'},{id:'bcn',label:'Barcelona'}]} value={city} onChange={setCity} />
```